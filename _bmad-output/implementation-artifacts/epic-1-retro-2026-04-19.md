# Epic 1 回顾报告

**日期：** 2026-04-19
**项目：** infitek_erp
**Epic：** Epic 1 — 项目基础设施与用户认证
**状态：** 6/6 stories 全部完成（100%）

---

## 交付指标

- 完成 Stories：1-1、1-2、1-3、1-4、1-5、1-6（共 6 个）
- 覆盖需求：FR-A01, FR-A02, FR-A03 + 全部 NFR 安全/可靠性要求
- 技术基础：Monorepo、NestJS、React、TypeORM、Docker、JWT 认证、OSS 集成

---

## 关键发现

### 亮点

1. **基础设施设计扎实** — 统一响应格式、全局 JWT 守卫、BaseEntity 审计字段、OSS 全局模块均已就绪，后续所有 Epic 可直接复用。
2. **Dev Notes 文档质量高** — Story 1.6 的 Dev Notes 包含架构约束、文件结构、关键代码模式，是良好的文档习惯，值得在后续 story 中坚持。

### 问题与改进点

1. **Story 文件状态未同步** — Story 1.1~1.4 的 `Status:` 字段停留在 `ready-for-dev` 或 `review`，未更新为 `done`，与 sprint-status.yaml 不一致。
2. **API 路径不一致** — FilesModule 使用 `/api/files/...`，其他模块使用 `/api/v1/...`。这是有意为之的设计（横切服务），但未在 architecture.md 中说明，容易引起混淆。

---

## 行动项

| # | 行动项 | 优先级 |
|---|--------|--------|
| A1 | 将 story 1.1~1.4 文件的 `Status:` 字段更新为 `done` | 低 |
| A2 | 在 architecture.md 补充说明 `/api/files/` 路径与 `/api/v1/` 的区别及原因 | 中 |
| A3 | dev-story 工作流规范：code review 通过后必须更新 story 文件 `Status:` 为 `done` | 中 |
| A4 | 后续所有开发必须遵循 UX 设计规范（`_bmad-output/planning-artifacts/ux-design-specification.md`），包括组件选型、交互模式、Design Token 主题、页面布局结构 | 高 |

---

## Epic 2 准备

**依赖检查：** Epic 1 的所有前置能力（JWT 守卫、统一响应、BaseEntity）均已就绪，可直接开始 Epic 2。

**关键提示：**
- Story 2.1（标准 CRUD 组件模板）是 Epic 2 的基石，需做得足够通用，覆盖列表页/详情页/表单页三态
- FR18/FR19/FR20（搜索、筛选、分页）是横切能力，首次在 Epic 2 落地，后续所有业务模块复用，值得投入时间做好
- **所有前端实现必须对照 `ux-design-specification.md` 执行**，确保组件选型、交互模式与设计规范一致

---

## UX 设计规范遵循要求

从 Epic 2 起，所有前端开发必须遵循以下规范（详见 `_bmad-output/planning-artifacts/ux-design-specification.md`）：

- **组件库**：使用 Ant Design 5.x，禁止引入其他 UI 框架
- **Design Token**：颜色、间距、字体使用规范中定义的 Token，不得硬编码
- **页面布局**：遵循规范中的侧边栏导航 + 内容区结构
- **列表页**：使用 ProTable 或 Table 组件，含搜索栏、筛选、分页
- **表单页**：遵循规范中的表单布局和校验提示规范
- **交互模式**：弹窗、抽屉、确认对话框等交互模式参照规范执行
- **错误提示**：遵循 NFR-U2，清晰说明出错原因，不暴露技术信息

---

*本回顾由 BMad 回顾工作流生成*
